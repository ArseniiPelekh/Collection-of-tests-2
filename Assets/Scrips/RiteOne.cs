using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RiteOne : MonoBehaviour {

    // Use this for initialization

    public int nam = 0;

	void Start () {
     //   mainCube = GameObject.Find("ButtonReturnAnswer"); 	
	}

    // Update is called once per frame
    private void OnMouseDown()
    {
        /*if (this.nam == mainCube.)
            mainCube.GetComponent<GameCntrla>().next = true;
        else
            mainCube.GetComponent<GameCntrla>().lose = true;*/
    }
}
